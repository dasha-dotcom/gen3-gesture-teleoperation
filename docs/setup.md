# Environment Setup

The final target environment has not yet been selected. Advisor confirmation is pending on whether to use a lab Ubuntu workstation/environment or a self-configured Ubuntu 24.04 + ROS 2 Jazzy setup. These versions are a candidate only, not a selected configuration.

The current development machine is a macOS personal computer. Only the local repository and documentation skeleton have been prepared. ROS 2, Ubuntu, MoveIt, ros2_control, ros2_kortex, Gazebo, MediaPipe, and other robotics dependencies have not been installed as part of this setup. No Kinova source code has been cloned or modified.

## Ubuntu Version

TBD — record the confirmed version and whether the environment is lab-provided or self-configured.

## ROS Version

TBD — record the confirmed ROS distribution and version information.

## Installation Steps

TBD — document verified installation steps after the target environment is confirmed. No installation commands are prescribed yet.

## Kinova Dependencies

TBD — record the ros2_kortex branch/commit, required dependencies, Gen3 6-DOF or 7-DOF configuration, and expected gripper model.

## MoveIt Dependencies

TBD — record the supported MoveIt version, configuration, and dependencies.

## Build Commands

TBD — record verified workspace build commands in the supported environment. `ros2_ws/src/` is currently only a source-directory placeholder.

## Sourcing Commands

TBD — record verified ROS and workspace sourcing commands once the environment exists.

## Troubleshooting

TBD — record observed errors, their causes, and verified fixes, including relevant version information.
